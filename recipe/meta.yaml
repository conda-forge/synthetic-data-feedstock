{% set name = "synthetic-data" %}
{% set version = "1.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/synthetic-data-{{ version }}.tar.gz
  sha256: f7ba7fa65821ba9e69c7a309c912c0431e5724f97cace4196af446dfbd44a60e

build:
  noarch: python
  script_env:
    - PYTHONUTF8=1
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - numpy >=1.21.0
    - scikit-learn >=1.1.2
    - scipy >=1.7.0
    - dataprofiler >=0.8.8

test:
  imports:
    - synthetic_data

about:
  home: https://github.com/capitalone/synthetic-data
  summary: Generates complex, nonlinear datasets for use with deep learning/black box models
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706
